# Legacy LibraryEntry Model (Deprecated)

This document preserves the original definition and documentation of the `LibraryEntry` model for reference and migration history. All usages should migrate to the unified `ServiceLibraryEntry` model.

---

```kotlin
// ...existing code from LibraryEntry.kt...
```

---

## Migration Notes
- All code should now use `ServiceLibraryEntry`.
- This file is retained for historical and migration reference only.
- Do not use in new code.
